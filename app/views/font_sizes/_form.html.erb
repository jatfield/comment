<%= form_for(font_size) do |f| %>
  <% if font_size.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(font_size.errors.count, "error") %> prohibited this font_size from being saved:</h2>

      <ul>
      <% font_size.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :value %>
    <%= f.number_field :value %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
