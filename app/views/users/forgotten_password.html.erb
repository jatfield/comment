<% if params[:token] %>
  <%= form_tag reset_password_users_path do %>
  <% if @user && @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= label_tag :password, "Jelszó" %>
    <%= password_field_tag :password %>
    <%= label_tag :password_confirmation, "Jelszó megerősítése" %>
    <%= password_field_tag :password_confirmation %>
    <%= hidden_field_tag :token, params[:token] %>
    <%= submit_tag "Jelszó beállítása" %>
  <% end %>

<% else %>

  <%= form_tag send_password_reset_link_users_path do %>
    <%= label_tag :username, "Felhasználónév" %>
    <%= text_field_tag :username %>
    <%= submit_tag "Küldés" %>
  <% end %>

<% end %>

